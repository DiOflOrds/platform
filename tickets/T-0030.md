---
id: T-0030
titel: "Kommentare an Aufgaben — Verlauf im Ticket-Rumpf wie im Team-Chat, ein Schreibweg, kein zweiter Speicher"
typ: change-request
prozess: swe3
rolle: dev
sprint: 25
status: open
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-20
geplant_sprint: 27
erstellt: 2026-08-20
---

## Herkunft

Brief `platform/N-0007` des Auftraggebers (2026-08-20 14:05):

> *„es soll möglich sein, zu den aufgaben direkte kommentare/antworten zu liefern. ähnlich
> wie hier beim Team-Chat"*

## Was der Vergleich mit dem Team-Chat entscheidet

Der Team-Chat ist **kein eigener Speicher**. Ein Brief ist eine Datei
(`management/briefkasten/N-xxxx.md`); Beiträge des Auftraggebers und Antworten des Teams
werden über `briefkasten._beitrag_anhaengen` **an dieselbe Datei angehängt**. Deshalb ist
ein Verlauf im Browser, im Terminal, in `git log` und am Handy derselbe Gegenstand.

Der bequeme Bau für Aufgaben wäre ein zweiter Ort — eine Kommentardatei je Ticket oder eine
Tabelle. Damit gäbe es ab dem ersten Tag **zwei Quellen** für „was steht an dieser
Aufgabe", neben Rumpf und Änderungshistorie.

> **Das ist B033, und dieses Haus hat es dreimal bezahlt (`p9/T-0007`, `pm/T-0017`,
> `platform/T-0028`). Der Brief nennt die richtige Bauart bereits: derselbe Ort wie beim
> Brief — die Datei selbst.**

## Definition of Done

1. **SWR** geschrieben (Nummer bei Baubeginn gegen HEAD prüfen), `reviewed` mit Nachweis.
2. Kommentarblock am Ticket-Detail im HMI: schreiben, absenden, sofort committet.
3. Der Schreibweg ist **derselbe** wie beim Ticket-Editor (`board.py`, Commit-Meldung
   „Mensch via HMI"). Keine zweite Regelprüfung im Server — die Lehre aus P10 Sprint 1.
4. Kommentare stehen im **Ticket-Rumpf**, unter einer eigenen Überschrift, mit
   `board.zeitpunkt` datiert (eine Zeitquelle, SWR-084).
5. Verlauf **neueste zuerst**, wie im Team-Chat bestellt (SWR-083).
6. ⚠ Auch an **erledigten** Aufgaben. Ein Kommentar ist keine Statusänderung; die Sperre
   für erledigte Tickets (SWR-077) gilt dem Formular, nicht dem Gespräch.
7. PIN-Gate wie an jedem Schreibpfad (SWR-081/160).
8. Konflikterkennung per Fingerabdruck wie beim Editor (SWR-080) — die Routine-Session
   schreibt in dieselben Dateien.
9. Gegenprobe im Test: ein Kommentar ändert **kein** Frontmatter-Feld (kein Status, keine
   Rolle, kein `geändert`-Sprung, der nach einer Bearbeitung aussieht).

## Zwei Punkte im Antrag, denen der Auftraggeber widersprechen kann

1. **Kommentare liegen im Ticket-Rumpf und gehen damit nach GitHub.** Das ist der Preis
   der einen Quelle und im Brief benannt.
2. **PIN-Gate**, obwohl der Auftrag es nicht nannte — ein Schreibpfad ohne Gate wäre der
   erste in diesem Haus.

## Termin

Sprint 25. ⚠ Wird er nicht geschlossen, steht der Grund **in diesem Ticket** und nicht
nur im Plan (`L-2026-08-17ag`).

---

## Sprint 25: NULLTE Verschiebung — angelegt und terminiert, nicht gebaut

Der Brief ist am selben Tag beantwortet, das Ticket steht mit fertigem Zuschnitt. Gebaut ist
es nicht: dieser Lauf ist vollständig in `platform/T-0029` gegangen — der Auto-Push-Wächter
stand seit drei Tagen (83 Abbrüche) und die 12 Ticks des Auftraggebers waren nie gelaufen.

⚠ **Die Reihenfolge ist nicht Bequemlichkeit, sondern derselbe Grund wie in B039:** solange
nichts nach außen geht, erzeugt jede Feature-Session Arbeit, die auf GitHub nicht existiert.
Der Kommentar-Verlauf wäre die vierte Schicht Arbeit hinter einer verschlossenen Tür
gewesen.

**Termin: Sprint 26**, Rolle `dev`. Der Zuschnitt oben ist vollständig; es fehlt nur der Bau.

---

## Sprint 26: ERSTE Verschiebung — Grund benannt, mit Verfallsdatum

Der Zuschnitt oben ist unverändert vollständig; es fehlt weiterhin nur der Bau.

**Was Sprint 26 stattdessen getan hat, nachzählbar:** **vier** Anforderungen geschlossen —
`SWR-167`/`SWR-168` (`platform/T-0031`: der Tick sagt, was er getan hat, und hinterlässt
HEAD, wo er ihn vorfand) und `SWR-169`/`SWR-170` (`platform/T-0032`: das Ollama-Modell aus
dem Besetzungsregister, mit Prüfung) — dazu die **Reparatur** des Schadens, den ein Tick um
20:15 im `platform`-Repo hinterlassen hatte, und die **Verbuchung** der Entscheidung `D004`.

⚠ **Die Reihenfolge ist dieselbe wie in Sprint 25 und aus demselben Grund (B039):** Der
Orchestrator hat an diesem Abend zum ersten Mal wirklich gearbeitet und dabei `main` und
einen Feature-Branch auseinandergetrieben. Ein Kommentar-Verlauf, der in **dieselben
Ticket-Dateien** schreibt wie die Routine-Session, wäre auf einem Repo gebaut worden,
dessen Schreibwege gerade nachweislich nicht verlässlich waren.

> **Ein zweiter Schreibweg in eine Datei ist erst dann eine Verbesserung, wenn der erste
> nachweislich dort landet, wo er soll.** Genau das ist mit `SWR-168` hergestellt worden —
> und deshalb ist die Reihenfolge kein Aufschub, sondern die Voraussetzung.

**Termin: Sprint 27**, Rolle `dev`. ⚠ Erste Verschiebung.
