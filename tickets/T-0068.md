---
id: T-0068
titel: "Der Schutz gegen Belege außerhalb des Bestands ist auf dem Rechner des Auftraggebers AUS — os.path.isabs beantwortet auf Windows eine andere Frage"
typ: problem
prozess: sup9
rolle: dev
sprint: 35
status: open
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-21
geplant_sprint: 36
erstellt: 2026-08-21
---

## Herkunft — gemessen im ERSTEN vollständigen Lauf der Teststrecke seit vier Sprints

Sprint 35 hat *„1551 Tests / 109 Testdateien"* als Verifikation berichtet und im selben
Atemzug eingeräumt, dass **1136 davon nicht ausgeführt** wurden (Sandbox-Ausfall ab
Block 2). Der Host hat die Strecke am 21.08. um 21:14 **vollständig** gefahren:

| Größe | Wert | Quelle |
|---|---|---|
| Tests gelaufen | **1551** in 210,4 s | `abschluss-logs/abschluss-20260821-211400.log:10102` |
| Rot | **6** | ebd. `:10104` (`FAILED (failures=6)`) |

> **⚠⚠ Die Zahl 1551 war vier Sprints lang die Zahl der GESAMMELTEN Zusicherungen und
> ist als Zahl der grünen berichtet worden. Beim ersten vollständigen Lauf sind sechs
> rot — und keine davon ist neu.**

## Der Befund dieses Tickets

`platform/backend/goldset.py:136`

```python
elif os.path.isabs(pfad) or ".." in pfad.split("/"):
    m.append(f"herkunft[{i}] ist kein repo-relativer Pfad: {pfad}")
```

Gemessen am Protokoll, nicht vermutet — `test_goldset.HerkunftTest` mit
`h=['/etc/passwd']` liefert auf dem Host die **leere** Meldungsliste:

```
AssertionError: False is not true : []
```

Die Prüfung hat `/etc/passwd` als **repo-relativen Pfad durchgewinkt.** Auf dem Mount
der Sandbox (POSIX) ist `os.path.isabs("/etc/passwd")` wahr; auf dem Rechner des
Auftraggebers ist es **falsch** — ein führender Schrägstrich ohne Laufwerksbuchstaben
gilt dort als laufwerksrelativ, nicht als absolut.

> **⚠⚠ Das ist kein roter Test. Das ist eine Schranke, die auf genau dem Rechner offen
> steht, auf dem sie gebraucht wird. Der Test ist die einzige Stelle, die es je gemeldet
> hat — und er konnte es erst melden, als jemand ihn dort laufen ließ, wo es zutrifft.**

Der Zweck der Schranke steht in ihrer eigenen Zusicherung: *„Ein Beleg ausserhalb des
Bestands ist fuer einen anderen Leser keiner."* Genau diese Zusicherung wirkt in der
Sandbox und nicht auf dem Host.

## Familie — die dritte Zusicherung, die ihre Umgebung misst statt ihren Gegenstand

Dieselbe Bauform hat Sprint 35 als **Befund 9** des Gegenlesens schon einmal gefunden
(*„Die Zusicherung hing an der Uhr des Läufers — unter `TZ=UTC` rot, ohne dass etwas
kaputt ist"*). Dort war die Umgebung die Zeitzone, hier ist sie das Betriebssystem;
`platform/T-0069` nennt die dritte (die Prozessliste).

⚠ **Der Unterschied zu Befund 9 ist der wichtige Teil und darf nicht eingeebnet werden:**
dort war der Test rot und der Code gesund. **Hier ist der Code krank und der Test war
grün** — vier Sprints lang, weil er nur dort lief, wo die Frage anders ausgeht.

## Zu klären, bevor gebaut wird (erst zählen, dann bauen)

1. **Wie viele weitere Stellen des Hauses fragen `os.path.isabs`, `os.sep` oder
   `os.path.join` in einer Prüfung, deren Antwort vom Betriebssystem abhängt?** Gezählt
   über den Bestand, nicht geschätzt. Eine Einzelreparatur an `goldset.py` wäre die
   Bauform aus `L-2026-08-17af` (*eine Reparatur, die nur ihren Fundort benutzt,
   repariert den Fundort*).
2. **Gibt es einen Beleg im echten `goldset.jsonl`, der die offene Schranke bereits
   passiert hat?** `promt-team/management/goldset.jsonl`, 51 belegte Fälle (SWR-149).
   Erst zählen: eine Schranke, die offen stand, ist etwas anderes als eine, durch die
   auch jemand gegangen ist.
3. **Wo läuft die Teststrecke künftig?** ⚠ Die Antwort „auf beiden" ist die teure und
   vermutlich richtige; sie gehört gemessen (Laufzeit 210 s) und nicht gewünscht.

## DoD

1. Stellen aus Vorabfrage 1 **gezählt** und namentlich im Ticket, bevor eine Zeile
   geändert wird.
2. Die Prüfung wird **betriebssystemunabhängig** — der Text `/etc/passwd` wird auf
   jedem Läufer abgewiesen. Gegenprobe: ein echter repo-relativer Pfad bleibt zulässig.
   ⚠ Nicht `ntpath`/`posixpath` nebeneinander: das wäre eine zweite Zählung für
   dieselbe Auskunft (B033).
3. Die Zusicherung wird so gebaut, dass sie **auf jedem** Läufer dieselbe Frage stellt —
   sonst ist die Reparatur wieder nur an einem Ort wirksam.
4. Vorabfrage 2 beantwortet: Zahl der bereits durchgelassenen Belege, auch wenn sie 0 ist.
5. Reviewer ≠ Autor.

## Termin

Sprint 36, Rolle `dev`, prio **hoch**. **Nullte Terminierung.**

⚠ Prio hoch und nicht kritisch: die Schranke schützt die **Herkunft von Goldset-Belegen**,
nicht ein Gate und keinen Zugang. Kritisch wäre eine Behauptung, die dieses Ticket nicht
belegen kann — Vorabfrage 2 entscheidet sie.
