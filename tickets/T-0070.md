---
id: T-0070
titel: "Acht Lehren ohne Vertreter — sechs davon aus Sprint 35, der im selben Bericht 1551 Tests als Verifikation führte"
typ: problem
prozess: sup8
rolle: coach
sprint: 35
status: open
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-22
geplant_sprint: 37
erstellt: 2026-08-21
---

## Herkunft — drei der sechs roten Zusicherungen des Host-Laufs vom 21.08. 21:14

```
FAIL: test_lehren_vertreter.SperrklinkeTest.test_keine_NEUE_lehre_ohne_vertreter
FAIL: test_lehren_vertreter.GrundmengeTest.test_erweitern_und_weglassen_sind_dasselbe_ergebnis
FAIL: test_lehrbuch_verliert_nichts.DieBasisAusSprint31HatteRecht.test_ohne_vertreter_ist_wieder_genau_die_basis
```

Alle drei nennen **dieselben acht Namen**:

`L-2026-08-21dj` · `dk` · `dl` · `dm` · `dn` · `do` · `dp` · `dq`

Die Sperrklinke sagt wörtlich, was zu tun ist: *„Entweder eine Zusicherung bauen, die die
Lehr-ID nennt — oder die Lehre bewusst als Beobachtung ohne Regel führen. Beides ist eine
Entscheidung; sie hier auszusitzen ist keine."*

**Sie ist seit dem 21.08. ausgesessen** — und nicht erst seit dem Shell-Ausfall:
**Sprint 35 hatte eine Shell und hat die Teststrecke nicht gefahren** (1136 der 1551
Zusicherungen blieben liegen, im eigenen Bericht eingeräumt). Die drei Folgeläufe konnten
sie nicht fahren.

## ⚠⚠ Der Befund ist nicht die Zahl, sondern die Herkunft

⚠⚠ **Die Erstfassung dieses Abschnitts war falsch, und das unabhängige Gegenlesen hat es
gefunden — nicht der Autor.** Sie schrieb *„alle acht hat der Lauf geschrieben, der keine
Shell hatte"*. Nachgezählt am Bestand:

| Lehre | „Anlass" laut Lehrbuch | Lauf hatte eine Shell? |
|---|---|---|
| `dj`, `dk`, `dl`, `dm`, `dn`, `do` | **Sprint 35** | **ja** — Sprint 35 hat gebaut (`SWR-213`, `platform/T-0065`) |
| `dp`, `dq` | Folgelauf, „Shell ausgefallen" | nein |

**Sechs von acht, nicht acht von acht.** Damit fällt die bequeme Erzählung („die Läufe
ohne Shell haben es angerichtet") und die unbequeme bleibt übrig:

> **⚠⚠ Die drei Zusicherungen waren schon am Ende von Sprint 35 rot — von dem Sprint,
> der im selben Bericht „1551 Tests" als Verifikationszeile führte und dazuschrieb, dass
> 1136 davon nicht gelaufen sind. Er hat sechs Lehren ohne Vertreter angelegt und die
> Prüfung, die genau das meldet, nicht ausgeführt.**

Die zwei Läufe ohne Shell haben den Befund dann **vergrößert** (zwei weitere Namen) und
konnten es nicht merken — Merken hätte die Shell gebraucht, die ihnen fehlte. Das ist
weniger, als die Erstfassung behauptet hat, und es ist genau, was belegbar ist.

⚠ Für die Bearbeitung heißt das: **die Herkunft der acht ist kein einheitlicher Fall.**
Sechs stammen aus einem Sprint mit Prüfstrecke, der sie nicht befragt hat; zwei aus
Läufen, die sie nicht befragen konnten.

`L-2026-08-21dq` ist einer der acht. Es ist die Lehre, die das Schreiben von Lehren ohne
Shell ausdrücklich freigegeben hat (*„Ohne `git` ist die Ticketdatei tabu, der Rest des
Hauses frei"*).

> **⚠⚠ Die Lehre, die das Schreiben von Lehren ohne Prüfstrecke erlaubt hat, steht selbst
> als einer der acht Namen in der Fehlermeldung, die genau davor warnt.**

⚠ Sie ist im selben Lauf berichtigt worden — **beide** Hälften sind widerlegt, die erste
(„Ticketdatei tabu") durch `abschluss.cmd` Schritt `[0/6]`, die zweite („Lehrdateien
frei") durch dieses Ticket. Der Nachtrag steht bei der Lehre selbst in
`process/knowledge/pl/lessons.md`; die Betriebsmittelfrage dahinter ist `pm/T-0086`
(Klasse A).

## Warum das nicht mit acht Zeilen erledigt wird

Ein Vertreter ist ein Vorkommen der Lehr-ID in einer `.py`/`.js`/`.cjs`-Datei
(`lehren.VERTRETER_ENDUNGEN`), außer in `lehren.py` und `test_lehren_vertreter.py`.
Acht Docstring-Zeilen an acht beliebige Zusicherungen zu hängen macht die drei Tests in
fünf Minuten grün — und wäre **genau der Fehler, gegen den `lehren.ohne_vertreter` in
seinem eigenen Rumpf warnt**:

> *„Eine Zitierung kann lügen; ein Schweigen kann es nicht. Diese Prüfung findet nicht
> die schlechte Zusicherung — sie findet die FEHLENDE."*

⚠ **Dieses Ticket ist kein Vertreter.** `.md` steht bewusst nicht in
`VERTRETER_ENDUNGEN` (`test_tickets_zaehlen_nicht_als_vertreter`): *„Ein Ticket ist ein
Vorsatz mit Datum; eine Zusicherung ist der Vollzug."* Die drei Tests bleiben durch das
Anlegen dieses Tickets rot, und das ist richtig so.

## Die Vorarbeit, die dieser Lauf leisten konnte — Sortierung nach Prüfbarkeit

Gelesen, nicht geraten. Vier der acht beschreiben eine Regel über **Code**, vier eine
Regel über **Prozess**:

| Lehre | Gegenstand | Kandidat für einen Vertreter |
|---|---|---|
| `dl` | Negativbeispiel ohne den Teil, an dem die Prüfung greift | die in Sprint 35 reparierten Gegenproben (`SWR-213`) |
| `dm` | ein Feld erbt den Wert seines Vorgängers | `test_fehler_nennt_seinen_provider` (`SWR-212`) |
| `dn` | der Wächter kennt nur die ältere Kopie | der in Sprint 35 auf **beide** Kopien erweiterte Zähltest |
| `do` | nicht nur die Klassifikation wandert, auch der WERT | die Zusicherung auf `beitraege()[].zeit` |
| `dj` `dk` `dp` `dq` | Sprintführung, Terminzählung, Verschiebung, Notbetrieb | **keiner** — Prozess, kein Code |

> **⚠ Für vier der acht existiert die Zusicherung vermutlich schon und trägt den Namen
> nicht. Für die anderen vier existiert sie nicht und wird es vielleicht nie — dann ist
> `**Beobachtung:**` die ehrliche Antwort und keine Ausrede.**

⚠ Das Wort **vermutlich** steht mit Absicht da: dieser Lauf hatte keine Shell und konnte
nicht nachsehen, ob die genannten Zusicherungen die Regel ihrer Lehre wirklich bewachen.
Ungeprüft angehängt wäre jede von ihnen eine Zitierung, die lügt.

## DoD

1. **Je Lehre einzeln entschieden** und die Entscheidung im Lehrbuch sichtbar: Vertreter
   oder `**Beobachtung:**`. Keine Sammelbehandlung.
2. Wo ein Vertreter benannt wird: **nachgewiesen**, dass die Zusicherung die Regel der
   Lehre bewacht — Mutationsprobe, nicht Augenschein.
3. Wächst `lehren.beobachtungen()` von 0, wird
   `test_kein_bestand_wird_still_als_beobachtung_geparkt` mit **Begründung**
   nachgezogen — die Zusicherung verlangt das ausdrücklich.
4. `OHNE_VERTRETER_BASIS` bleibt **unverändert**. ⚠ Die Basis nachzuziehen macht alle
   drei Tests in einer Minute grün und löscht den Befund; genau diese Zurückhaltung hat
   in Sprint 34 **90 Lehren gerettet**.
5. Reviewer ≠ Autor.

## Termin

Sprint 36, Rolle `coach`, prio **hoch**. **Nullte Terminierung.**

⚠ Rolle `coach`: die Lehrbücher sind sein Gegenstand. `dev` würde die vier Code-Lehren
sauber anschließen und die vier Prozess-Lehren stehen lassen — und die vier
Stehengebliebenen sind der eigentliche Fall.

---

## ⚠ Zwischenstand 22.08. (Host-Lauf des Auftraggebers): alle acht INTERIM als Beobachtung gebucht

Der Auftraggeber hat die Wiederherstellung des Takts angeordnet; die drei roten
Zusicherungen blockierten seit vier Tagen jeden Abschluss-Push. Gewählt wurde der
**zweite** Ausgang der Sperrklinke: alle acht tragen jetzt den `**Beobachtung:**`-Marker
im Lehrbuch, und `test_kein_bestand_wird_still_als_beobachtung_geparkt` ist mit
Begründung auf genau diese acht nachgezogen (DoD 3 damit erfüllt; `OHNE_VERTRETER_BASIS`
**unverändert**, DoD 4 gewahrt).

⚠ **Das ist ausdrücklich ein Zwischenstand, keine Erledigung von DoD 1/2:** die
Einzelentscheidung je Lehre steht aus. Für `dj`/`dk`/`dp`/`dq` dürfte die Buchung laut
der Tabelle oben bereits die ehrliche Endantwort sein; für `dl`/`dm`/`dn`/`do` bleibt
der Auftrag, die benannten Vertreter-Kandidaten mit **Mutationsprobe** nachzuweisen —
wer das tut, entfernt Marker und Listeneintrag im selben Zug. Der Wortlaut aller acht
Lehren ist unangetastet.


## Termin nachgezogen: Sprint 37 (2026-08-22, Abschluss Sprint 36)

Nicht in Sprint 36 geschlossen. Der Grund steht im Abschluss von
`pm/management/sprint-aktuell.md`; der Termin steht **hier**, weil eine Verschiebung erst
verschoben ist, wenn das Ticket sie traegt (`L-2026-08-17ag`, `L-2026-08-21dp`).

⚠ Nachgezogen **vor** `--beende`, nicht danach: `sprint_vergangen` (SWR-112) vergleicht
gegen den laufenden Sprint und haette sonst jedes dieser Tickets als Befund gemeldet —
genau die Lawine, die den Schnelltakt dreimal in Folge gesperrt hat.
