---
id: T-0048
titel: "Der Schnelltakt prüft die Besetzung ERST NACH der Auswahl — 2 von 2 Ticks im ersten startklaren Lauf ohne Ergebnis, obwohl der Preflight repariert ist"
typ: problem
prozess: sup9
rolle: dev
sprint: 30
status: done
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-21
geplant_sprint: 30
erstellt: 2026-08-21
---

## Herkunft — die Vorhersage aus Sprint 29 ist eingetreten, und dahinter stand ein zweiter Blocker

Sprint 29 hat `SWR-191` gebaut und dazu ausdrücklich geschrieben:

> *„Der Abbruch aus diesem Grund sollte aufhören. **Das ist eine Vorhersage und keine
> Messung** — eine Cowork-Session kann deinen Takt nicht auslösen."*

**Die Vorhersage ist eingetreten und ist jetzt eine Messung.** Gemessen an
`ollama-schnelltakt.log` (Zeilen 8127 ff., Lauf vom 21.08. um 04:15:00 — der erste Lauf
nach dem Commit von `SWR-191` um 04:10):

| Lauf | `platform` | `team-mail` |
|---|---|---|
| bis 04:00:00 (65 Ticks) | `Tick abgebrochen: Preflight hat Befunde (T-0024).` | dito |
| **04:15:00** | **`PREFLIGHT: STARTKLAR (4 fortgeschrieben)`** | **`PREFLIGHT: STARTKLAR (4 fortgeschrieben)`** |

**Der Takt bricht nicht mehr am Preflight ab. Er tut trotzdem nichts** — und der Grund ist
ein anderer:

```
Gewählt: T-0001 'Takt: Werkzeug- und Plattformpflege …' — Rolle CM, Route: llm (['ollama'])
Tick OHNE ERGEBNIS (Besetzung): Rolle CM hat in Einheit 'platform' keine Besetzung mit
motor 'ollama' … Mit motor 'ollama' besetzt: MAIL-RED@team-mail, PROB@platform.
```

**2 von 2 Ticks** dieses Laufs endeten so (`platform` → Rolle CM, `team-mail` → Rolle DEV).

## ⚠⚠ Der Befund: eine Prüfung NACH der Auswahl ist kein Filter, sondern ein Veto gegen genau einen Kandidaten

`waehle_ticket()` (`platform/orchestrator/tick.py`) bildet die Kandidatenmenge aus
`status: open` + Blocker erledigt + Rolle aktiv/ki, sortiert nach Prio/ID und gibt
**`kandidaten[0]`** zurück. Erst danach — im Rumpf von `tick()` — läuft
`besetzungsbefund()` und bricht ab.

> **Damit sieht der Tick genau einen Kandidaten an. Trägt der oberste keine Rolle mit
> Ollama-Besetzung, endet der Lauf — die übrigen Kandidaten werden nie geprüft. Ein
> Filter, der nach der Auswahl läuft, filtert nicht; er legt gegen den Sieger ein Veto ein
> und lässt die Zweitplatzierten ungesehen.**

⚠ **Das ist kein Vorwurf an `SWR-171`.** Die Prüfung steht bewusst vor Gateway-Aufruf,
Branch und Statuswechsel, und dort gehört sie hin — sie ist die letzte Verteidigungslinie
und muss auch bei erzwungener Auswahl (`--ticket`) greifen. Was fehlt, ist die **erste**:
dieselbe Frage in der Auswahl, wo sie über die Menge entscheidet statt über ein Exemplar.

## ⚠ Und die Meldung ist wahr, aber zu eng — genau das ist teuer

Der Satz *„Rolle CM hat in Einheit 'platform' keine Besetzung mit motor 'ollama' …
T-0001 bleibt unangetastet"* liest sich wie ein **Zufall**: *dieses eine Ticket* passte
nicht. Gemessen ist aber ein **Zustand**:

**0 von 8 offenen Tickets der gesamten Organisation tragen eine Rolle mit
Ollama-Besetzung** (Messung 2026-08-21, alle Repos):

| Rolle im offenen Ticket | Anzahl | ollama-besetzt? |
|---|---|---|
| `cm` (platform/T-0001, T-0047) | 2 | nein |
| `pl` (pm/T-0001, T-0002, T-0077, team-dashboard/T-0001) | 4 | nein |
| `coach` (pm/T-0003) | 1 | nein |
| `dev` (team-mail/T-0001) | 1 | nein |
| **ollama-besetzt sind:** `PROB@platform`, `MAIL-RED@team-mail` | — | **0 Treffer** |

> **⚠⚠ Der Unterschied zwischen „der oberste passte nicht" und „hier passt keiner, und
> daran ändern 96 Läufe am Tag nichts" ist der ganze Befund. Die erste Aussage lädt zum
> Wiederkommen in 15 Minuten ein — und genau das ist 90 Ticks lang passiert. Die zweite
> sagt, dass Warten die falsche Handlung ist.**
>
> Sprint 29 hat gelernt: *ein falscher Befund ist teurer als kein Befund*
> (`L-2026-08-21ce`). Dies hier ist der Zwilling: **eine wahre, aber zu enge Meldung über
> einen strukturellen Zustand erneuert bei jedem Lauf die Hoffnung auf eine andere
> Antwort.** Sie trainiert dasselbe Wegsehen mit besserem Gewissen.

⚠ Der Docstring von `waehle_ticket()` **hat diesen Zustand bereits am 20.08. gemessen**
(„kein einziges der 14 offenen Tickets trägt eine Rolle mit ollama-Besetzung") — und die
Zahl steht **im Kommentar einer Funktion**, nicht in der Meldung, die der Betrieb 90-mal
gedruckt hat. Heute nachgemessen: **0 von 8**. Der Zustand hält, die Grundmenge ist
kleiner geworden.

## Was dieses Ticket NICHT tut

⚠⚠ **Es legt keinen Arbeitsvorrat an und ändert keine Besetzung.** Genau das ist die
Frage, die `pm/T-0077` dem Auftraggeber gestellt hat (Frist 28.08., Default **A** =
alles bleibt). Eine Besetzung hier zu ergänzen wäre die Antwort **B**, im Vorgriff und
von der falschen Seite — Besetzungen pflegt das PM-Team als Klasse B, und diese hier hängt
an einer offenen Auftraggeber-Entscheidung.

> **Dieses Ticket macht den Zustand nur wahr aussprechbar. Ob er sich ändern soll,
> entscheidet `T-0077`.**

Es entsperrt auch **nicht** `pm/T-0071` (`blocked_by: [T-0077]`): dessen Schritt 2 fragt
*„werden nur ollama-fähige Typen gezogen?"*, und diese Frage bleibt unbeantwortbar,
solange es nichts zu ziehen gibt. Was sich ändert: der Grund dafür steht danach **im
Log** statt in einem Docstring.

## Zu bauen

1. **Der Besetzungs-Filter wandert in die Auswahl.** `waehle_ticket()` bekommt die
   Besetzungsfrage als Kandidaten-Kriterium — geprüft wird **jeder** Kandidat, nicht der
   oberste.
2. **`besetzungsbefund()` bleibt, wo es steht.** Es deckt den erzwungenen Fall
   (`--ticket`) ab, den die Auswahl nicht sieht. Zwei Stellen, zwei verschiedene Fragen:
   *„welche Kandidaten kommen infrage"* und *„darf dieser eine bearbeitet werden"*.
   ⚠ Das ist ausdrücklich **kein** B033: es ist eine Frage an zwei Mengen, nicht zwei
   Kopien einer Antwort. Beide rufen **dieselbe** Funktion — der Ausdruck existiert
   einmal.
3. **Die Meldung trägt die Zahl.** Findet die Auswahl keinen Kandidaten mit passender
   Besetzung, nennt sie, **wie viele** offene Tickets geprüft wurden, **welche Rollen**
   sie tragen und **welche Instanzen** in dieser Einheit mit dem Motor besetzt sind — eine
   Aussage über den Bestand statt über ein Exemplar (`SWR-167`: das Ergebniswort trägt
   den Grund; `SWR-174`: die Zahl trägt ihren Zeitpunkt).
4. **Gegenprobe an einer synthetischen Wurzel** (`L-2026-08-20cm`: nicht an den 17
   Live-Repos mutieren, die eine fremde Automatik alle 15 Minuten anfasst): ein Repo mit
   einem Ticket der besetzten Rolle **hinter** einem Ticket ohne Besetzung — die Auswahl
   muss das hintere finden. Ohne die Reparatur findet sie nichts. **Genau diese Gegenprobe
   ist der Beleg, dass der Filter ein Filter ist und kein Veto.**

## DoD

Filter in der Auswahl · `besetzungsbefund` unverändert als zweite Linie · Meldung nennt
geprüfte Anzahl, Rollen und besetzte Instanzen · Gegenprobe „hinterer Kandidat wird
gefunden" grün · Gegenprobe „ohne Besetzung kein Kandidat" grün · Anforderung in
`p9/requirements/` vor dem Bau · Reviewer ≠ Autor.

## Termin

Sprint 30, Rolle `dev`. **Nullte Terminierung** — das Ticket entsteht mit der Messung.

---

## Sprint 30: gebaut — `SWR-196`, und die Wirkung ist am echten Bestand gemessen

Der Filter steht in `waehle_ticket()` **vor** der Sortierung; `besetzungsbefund()` ist
unverändert geblieben. Gemessen an den Live-Repos (`--dry-run`, kein Statuswechsel):

| Einheit | vorher | nachher |
|---|---|---|
| `platform` | `Gewählt: T-0001 …` → `Tick OHNE ERGEBNIS (Besetzung): Rolle CM … T-0001 bleibt unangetastet.` | `Kein bearbeitbares Ticket (Besetzung): **2** offene(s) Ticket(s) geprüft, keines trägt eine Rolle mit motor 'ollama' … Rollen im Bestand: **CM**; besetzt in dieser Einheit: **PROB@platform** … ein weiterer Lauf ändert das nicht.` |
| `team-mail` | `Gewählt: T-0001 …` → `… Rolle DEV …` | `… **1** offene(s) Ticket … Rollen: **DEV**; besetzt: **MAIL-RED@team-mail** …` |

> **Der Tick wählt kein Ticket mehr, das er nicht bearbeiten darf. Er sagt jetzt, wie
> viele er angesehen hat — und dass Warten die falsche Handlung ist.**

⚠ **Was sich NICHT geändert hat und sich auch nicht ändern sollte: es wird weiterhin
nichts gearbeitet.** Die Reparatur macht den Zustand aussprechbar, sie hebt ihn nicht auf.
Das kann nur `pm/T-0077`.

### ⚠ Ein Eingriff in bestehende Zusicherungen — benannt statt stillschweigend

`waehle_ticket()` gibt jetzt `(ticket, befund)` zurück. Das hat **9 Aufrufstellen** in
`test_orchestrator.py` (6) und `test_besetzung_am_aufrufer.py` (3) berührt.

> **Geändert wurde ausschließlich die ENTPACKUNG (`…[0]` bzw. `t, _ =`). Keine einzige
> Zusicherung hat einen anderen Inhalt bekommen** — `SWR-016`/`SWR-018`/`SWR-172` prüfen
> Wort für Wort dasselbe wie vorher. Der Unterschied zwischen „Tests an den Code
> angepasst" und „Entpackung nachgezogen" ist der ganze Punkt, und er steht hier, damit
> ihn jemand nachprüfen kann statt ihn zu glauben.

### Gegenproben

`test_tick_besetzungsfilter.py`, **11 Zusicherungen** an einer synthetischen Wurzel
(`L-2026-08-20cm` — nicht an den 17 Live-Repos, die eine fremde Automatik alle 15 Minuten
anfasst). Die tragende ist `test_hinterer_kandidat_wird_gefunden`: das besetzte Ticket
steht **hinter** dem unbesetzten, und nur ein Filter findet es. **Alle anderen wären auch
bei einem Veto grün** — deshalb steht sie einzeln benannt da.

Dazu: die zweite Linie greift weiter (`--ticket`), beide Linien werden aneinander
gehalten (Widerspruch = rot), die Grenze „ohne `--provider`/ohne Wurzel unverändert" ist
zugesichert, und `„nichts offen"` bleibt von `„nichts Besetztes offen"` unterscheidbar.

**52 Tests** der drei betroffenen Module grün, Matrix **196 SWRs / 0 Lücken**.

## DoD-Abgleich

| # | Punkt | Stand |
|---|---|---|
| 1 | Filter in der Auswahl | ✅ vor der Sortierung, jeder Kandidat |
| 2 | `besetzungsbefund` als zweite Linie unverändert | ✅ mit eigener Zusicherung belegt |
| 3 | Meldung nennt Anzahl, Rollen, besetzte Instanzen | ✅ am echten Bestand gemessen |
| 4 | Gegenprobe „hinterer Kandidat" grün | ✅ |
| 5 | Gegenprobe „ohne Besetzung kein Kandidat" grün | ✅ |
| 6 | Anforderung vor dem Bau | ✅ `SWR-196`, Commit `d067a1a` in `p9` |
| 7 | Reviewer ≠ Autor | ✅ `qm` laut Ticketfeld |

## Review (`qm`, Reviewer ≠ Autor laut Ticketfeld)

**Prüfbarkeit:** die Anforderung nennt eine Bauform (*Filter vor der Sortierung*) und
einen Ausschluss (*zweite Linie bleibt*) — beides ist an einer Zusicherung aufgehängt und
nicht an Zustimmung.

**Trennschärfe:** die tragende Gegenprobe ist als solche **benannt** und nicht nur
vorhanden. Der Satz *„alle anderen wären auch bei einem Veto grün"* ist der Prüfstein
dieses Reviews: er sagt vorab, welche der elf Zusicherungen etwas beweist. `SWR-190`
verlangt genau das.

**Ein Einwand, aufgenommen statt abgetan:** der Ertrag ist eine **Meldung**, und Meldungen
sind billig zu behaupten. Deshalb ist der Vorher/Nachher-Vergleich am **echten Bestand**
gefahren worden (`--dry-run`) und steht mit beiden Wortlauten im Ticket — nicht als
Versprechen, sondern als Abschrift.

**Ein Restpunkt, benannt:** `waehle_ticket()` gibt jetzt ein Tupel zurück, und das hat
neun fremde Aufrufstellen berührt. Der Bericht behauptet, nur die Entpackung geändert zu
haben. **Nachgeprüft an der Differenz:** die geänderten Zeilen tragen ausschließlich
`[0]` bzw. `t, _ =`; keine `assertEqual`-Erwartung und kein Docstring einer bestehenden
Zusicherung ist berührt. Der Einwand war berechtigt und ist ausgeräumt.

**Kein Befund.** Freigabe.
