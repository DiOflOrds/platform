---
id: T-0041
titel: "Folge-CR (Konzept 04 Kap. 7): typ-Literale semantisch nachziehen — aspice→plattform, teams→projekte (Backend + Tests, ein Zug)"
typ: change-request
prozess: sup10
rolle: chg
sprint: 0
status: done
prio: niedrig
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-21
erstellt: 2026-08-21
---

## Anlass (Link)

Orga-Rework 2 (pm/T-0073): Die Organisation kennt nur noch PM + Projekte, aber die
Registry-Literale sagen es noch nicht — `teams:`-Schlüssel, `typ: aspice`. Bewusst NICHT
nebenbei geändert, weil `aggregation.einstufung` (Zeile ~202: `typ in ("aspice", "pm")`)
und mindestens drei Tests (`test_anzeigename_einheit`, `test_org_cockpit` ×2) auf die
Literale prüfen (Konzept 04, Kap. 7).

## Wunsch

In **einem** Zug: `teams:` → `projekte:` (oder dokumentiert beibehalten), `typ: aspice` →
`typ: plattform`, Konsumenten (`aggregation.py`, `organigramm.py`, `teams.py`,
`organisation.py`) und alle betroffenen Tests. Übergangsregel: alte Literale eine
Ausbaustufe lang lesend akzeptieren (Migration ohne Stichtag), Schreiben nur neu.

## Impact (CHG orchestriert)

Betroffen: process/teams/registry.yaml (Schema-Kopf), Backend-Leser, Tests, evtl.
Cockpit-Gruppierung im HMI. Keine Baseline-Wirkung; kein Verhalten für den Menschen —
reine Begriffs-Hygiene. Deshalb prio niedrig: **erst einplanen, wenn ohnehin an der
Einstufung gearbeitet wird** (Huckepack-Regel; ein eigener Sprint dafür wäre Umbenennung
um der Umbenennung willen).

## DoD

Alle Leser + Tests auf neue Literale · alte Werte lesend toleriert + deprecation-Vermerk ·
Suite grün · Registry-Kopf ohne die Warnbox aus Konzept 04 Kap. 7.

## Ergebnis (2026-08-21) — in EINEM Zug umgestellt, mit Toleranz statt Stichtag

Der Huckepack-Anlass kam frueher als gedacht: der Auftraggeber hat die Erledigung aller
Folge-CRs beauftragt. Umgestellt: `aggregation.einstufung` normalisiert (`plattform` →
Gruppenlogik von `aspice`; genau EINE Zuordnungszeile, kein Register von Sonderfaellen),
`platform/team.yaml` + Projekt-Registry tragen `typ: plattform`, die team.yaml-Namen der
drei umgewidmeten Projekte sagen jetzt P13/P14/P15. Alte Literale bleiben eine
Ausbaustufe lang lesend toleriert.

Verifikation als PAAR: `test_typ_literale` 3/3 NEU (plattform → festes-team; aspice
weiterhin → festes-team; Core-Team-Expansion bei typ plattform) UND die Alt-Fixtures
(test_org_cockpit, test_anzeigename_einheit) laufen unveraendert gruen als
Toleranz-Beleg — 78 Tests im Batch, OK. Der Registry-Kopf traegt die neue Regel; der
Top-Level-Schluessel `teams:` bleibt dokumentiert bestehen (kein Leser-Nutzen einer
Umbenennung — im DoD als Option ausdruecklich vorgesehen).
