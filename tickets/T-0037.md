---
id: T-0037
titel: "Projektmodell (pm/T-0073): effektive_besetzungen-Resolver, Core-Team-Expansion in SWR-169/170/171-Wegen, projekt_setup.py, HMI-Core-Kennzeichnung"
typ: change-request
prozess: swe3
rolle: dev
sprint: 27
status: done
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-21
erstellt: 2026-08-21
---

## Ziel

Code-Anteil des Orga-Rework 2 (Konzept `process/docs/04-projektmodell-orga-rework-2.md`,
Governance-CR `pm/T-0073`): Das Core Team gilt implizit je aktivem Projekt — es braucht
**einen** Resolver, den alle Leser teilen.

## Geliefert

1. `organigramm.py`: **`effektive_besetzungen(root)`** (Core-Expansion + explizite Einträge,
   explizit gewinnt; PM-Team ausgenommen; nur aktive Einheiten). `sammle()` nutzt ihn;
   Mermaid zeigt Core als einen Knoten, Tabellen tragen eine Quelle-Spalte.
2. `organisation.py`: `modell_der_besetzung` (SWR-169), `modellabweichungen` (SWR-170),
   `besetzung_mit_motor`/`besetzungen_mit_motor` (SWR-171), `detail`, `anlegen`-F20 lesen
   die **effektive** Besetzung; `setzen` **materialisiert** implizite Core-Instanzen als
   Abweichungs-Block; `entfernen` lehnt Core-Instanzen mit Handlungsanweisung ab (400).
   ⚠ Damit bleibt der Sprint-27-Befund geheilt UND korrekt: `DEV@team-mail` existiert nun
   implizit (`motor: cowork`) — für einen `--provider ollama`-Tick weiterhin „keine
   Besetzung mit diesem Motor".
3. `projekt_setup.py` (neu) + `process/templates/projektplan.md`: kompletter Projekt-Scaffold
   mit 12 board-validen Tickets (Planung → 9 Rollen-Initialisierungen → 2 Takt-Tickets).
4. `static/organisation.html`: Core-Chips (grau, klickbar → Detail), Speichern-Beschriftung
   „erzeugt Abweichung", Entfernen-Knopf bei Core ausgeblendet.

## Verifikation

`tests/test_organisation.py` 11/11 (neu: Core-Expansion, explizit-gewinnt, Materialisierung,
Entfernen-Sperre 400, Anlegen-Hinweis 409) · `tests/test_projekt_setup.py` 3/3 (neu) ·
`tests/test_organigramm.py`, `tests/test_anzeigename_einheit.py` grün (21 gesamt) ·
`organigramm.py --check` grün · Server-Smoke (Core-Detail, Katalog) · `node --check` grün.

## Ehrliche Grenzen

- SWR-Nachtrag für Resolver, Materialisierung und projekt_setup fehlt (RM-Ticket) — wie bei
  T-0028 nachziehen, nicht vergessen.
- `MOTOR_JE_PROVIDER` bleibt absichtlich unvollständig (nur ollama) — unverändert aus T-0033.
- Volle Testsuite lief in der Sandbox nicht (Mount-Tempo); im nächsten Sprint-Lauf am PC.
