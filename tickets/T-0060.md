---
id: T-0060
titel: "Ollama-Takt: EIN echter Lauf mit dem tatsaechlich vorhandenen Modell — die 11 Fehlschlaege liegen alle VOR der Reparatur"
typ: task
prozess: sup8
rolle: dev
sprint: 33
status: in_review
prio: hoch
blocked_by: []
repo: platform
reviewer: qm
geändert: 2026-08-21
geplant_sprint: 35
erstellt: 2026-08-21
---

## Herkunft — `pm/D029` = C, vom Auftraggeber am 2026-08-21 um 10:41 entschieden

`pm/T-0084` fragte, was mit vier Aufgaben geschieht, die auf ein **entschiedenes** Ticket
zeigen. Antwort: **Option C** — sie bekommen eine **offene** Aufgabe, auf die sie zeigen
können. Das ist dieses Ticket.

Der Auftraggeber hat der Frage außerdem eine Tatsache mitgegeben, die wir nicht hatten:

> *„also ollama modell gemma3.27b ist aktuell, kein llama3.1:8b."*

## ⚠⚠ Und die Nachmessung hat unsere eigene Diagnose umgedreht

Bis heute stand in Plan, Bericht und Entscheidungsvorlage: *„die Run-Registry zeigt
ausschließlich `status: fehler` (`model 'llama3.1:8b' not found`)"*. Das ist **richtig
gelesen und falsch gedeutet**. Nachgemessen an den Zeitstempeln:

| Größe | Wert |
|---|---|
| Ollama-Läufe mit `status: fehler` | **11** |
| davon nach dem 2026-08-20 **19:45** | **0** |
| Reparatur der Modellauflösung (`platform/T-0032`, `SWR-169`) committet | 2026-08-20 **20:45** |
| Ollama-Läufe **seit** der Reparatur | **0** |
| erfolgreiche Ollama-Läufe überhaupt | **1** — `p0`, 2026-08-06, Modell **`ollama/gemma3:27b`** |

> **⚠⚠ Sämtliche Fehlschläge liegen VOR der Reparatur, die genau sie behoben hat. Seit
> der Reparatur hat es keinen einzigen Versuch gegeben — wir haben eine Stunde alte
> Messung fünf Sprints lang als aktuellen Zustand zitiert.**
>
> **Eine Fehlerliste ohne Zeitachse ist keine Diagnose. Sie ist ein Archiv, das sich wie
> ein Befund liest.**

Und die Registry belegt den Auftraggeber: der eine erfolgreiche Lauf trug **`gemma3:27b`**
— genau das Modell, das `process/roles/besetzungen.yaml` Zeile 77 seit dem 06.08. führt.
`llama3.1:8b` war nie mehr als der Rückfallwert in `guardrails.yaml`.

## Was zu tun ist — und in welcher Reihenfolge

1. **Erst messen, dann bauen.** Ein einziger echter Lauf
   (`python platform/orchestrator/tick.py --repos . --projekt platform --provider ollama
   --rolle dev`) beantwortet die Frage, die fünf Sprints lang vermutet wurde. ⚠ **Nichts
   reparieren, bevor dieser Lauf gelaufen ist** — es gibt derzeit keinen Beleg, dass noch
   etwas kaputt ist.
2. **Den heutigen Blocker benennen, nicht überspringen.** Der Schnelltakt bricht seit
   Tagen an *Preflight-Befunden* ab (`Tick abgebrochen: Preflight hat Befunde`), nicht am
   Modell — das ist `platform/T-0052`/`SWR-201`-Gebiet und eine **andere** Ursache als
   die, die hier untersucht wird. Beide auseinanderzuhalten ist Teil der Aufgabe.
3. **Erst wenn der Lauf `status: ok` **und** ein Artefakt liefert**, ist der Nachweis aus
   `pm/T-0071` erbracht und der Ollama-Offload darf Tickets übernehmen.
4. `guardrails.yaml` trägt weiterhin `llama3.1:8b` als Default. Ob das nach dem Lauf ein
   Befund ist oder ein harmloser Rückfall, **ergibt die Messung** — nicht dieses Ticket.

## Warum das die vier gesperrten Aufgaben entsperrt

`pm/T-0071`, `pm/T-0079`, `promt-team/T-0003` und `promt-team/T-0012` zeigen ab jetzt
hierher statt auf das geschlossene `pm/T-0077`. Damit ist ihre Sperre wieder **ehrlich**:
sie verweist auf etwas Offenes, an dem jemand arbeiten kann.

## DoD

Ein echter Ollama-Lauf durchgeführt und im Register belegt (`status`, `modell`,
`meldung`) · Ergebnis **gemessen** statt vermutet · Ursachen „Modell" und „Preflight"
getrennt benannt · bei Befund: eigener Bau mit Zusicherungs-Paar · Reviewer ≠ Autor.

## Termin

Sprint 34, prio **hoch**. ⚠ **Nicht Sprint 33:** der Lauf braucht den echten Team-Node
(`DESKTOP-8OOO6JS`) mit laufendem Ollama-Dienst; diese Session läuft in einer Sandbox
ohne ihn. Ihn hier zu „versuchen" hieße, einen Umgebungsfehler als Messergebnis zu
verbuchen — genau der Fehler, den dieses Ticket gerade aufdeckt.

---

## ⚠⚠ Verschoben nach Sprint 35 — und der Grund ist NICHT Kapazität, sondern der Ort

**Dieser Lauf konnte den Nachweis nicht führen: Ollama ist von hier aus nicht erreichbar.**
Gemessen, nicht vermutet:

```
curl -s -m 4 http://localhost:11434/api/tags        -> keine Antwort
curl -s -m 4 http://host.docker.internal:11434/...  -> Connection blocked by network allowlist
```

Die Routine-Session läuft in einer Sandbox mit Netz-Allowlist; der lokale Ollama-Dienst
liegt auf dem Rechner des Auftraggebers.

> **Ein Nachweis, der einen fremden Rechner braucht, wird nicht dadurch geführt, dass man
> ihn ein weiteres Mal einplant. Er braucht eine Handlung dort — und das steht ab jetzt
> als das, was es ist, statt als Verschiebung.**

⚠ **Vier Tickets hängen daran** (`pm/T-0071`, `pm/T-0079`, `promt-team/T-0003`,
`promt-team/T-0012`). Ihre Sperre ist damit **korrekt** im Sinne von `SWR-204`: sie zeigt
auf ein offenes Ticket mit einem echten, benannten Hindernis — und nicht auf Altpapier.

⚠ **Zweite Verschiebung.** Bei der vierten Berührung wird entschieden statt terminiert:
entweder der Lauf findet auf dem Team-Node statt, oder der Ollama-Weg wird geschnitten und
die vier abhängigen Tickets werden anders zugeschnitten. Das ist dann eine Entscheidung
und keine Planungszeile.

---

## Sprint 35 — dritte Terminierung: der Blocker ist geräumt, der Nachweis ist MÖGLICH gemacht

⚠⚠ **Die Diagnose war zum vierten Mal in vier Sprints eine andere — und diesmal war der
MESSPUNKT der Fehler.** Sprint 34 hat aus der Sandbox gemessen (`localhost:11434` tot,
`host.docker.internal` von der Allowlist gesperrt) und daraus geschlossen, der Nachweis
brauche einen fremden Rechner. Beides stimmt. Beides misst die falsche Maschine.

**Der Takt läuft. Er läuft seit dem 20.08. auf `DESKTOP-8OOO6JS`, und sein Protokoll liegt
im Arbeitsordner** (`ollama-schnelltakt.log`, 842 KB, zuletzt geschrieben 14:46 **heute**).

| Größe | Gemessen |
|---|---|
| Schnelltakt-Läufe seit 20.08. 17:15 | **87** |
| Abbrüche, alle mit **derselben** Meldung | **138** |
| Erfolgszeilen | **0** |
| Ticks mit `status: ok` in allen drei Run-Registries | **0** |

> **⚠⚠ Drei Sprints lang haben wir die Erreichbarkeit einer Maschine diskutiert, auf der
> der Takt gar nicht läuft — während sein echtes Protokoll unangetastet danebenlag und
> 87 Mal dasselbe sagte.**

### Die Zeitachse, gemessen statt erzählt

| Zeit | Ereignis |
|---|---|
| 20.08. 20:44 | `SWR-167..170` — die „Reparatur", auf die sich Sprint 33 berief |
| 20.08. 21:15 / 21:30 / 21:45 | **6 Versuche, alle `404 llama3.1:8b`** — Sprint 33 schrieb „seither kein einziger Versuch" |
| 20.08. 22:05 | `T-0033` → `done`: Besetzung wird **vor** dem Aufruf geprüft |
| seit 22:05 | **138 Abbrüche, 0 Versuche.** Der eigentliche Fix ist **nie ausgeführt worden** |

### ⚠⚠ Und die 138 Abbrüche waren unsere eigene Buchführung

Alle 138 tragen wörtlich dieselbe Zeile: `Tick abgebrochen: Preflight hat Befunde
(T-0024).` Der Preflight-Lauf von 14:46 nennt **zwei** Befunde, und **beide hat Sprint 34
selbst erzeugt**: vier Planzeilen mit der alten Sprintnummer, neun `team-termine`-Tickets
ohne Sprint (die Projektgründung desselben Sprints).

> **Unser eigener Sprint-Abschluss hat den Nachweis blockiert, den derselbe Sprint als
> „nicht führbar" gemeldet hat. Die Sperre trug den Namen der Sorgfalt.**

**Beides ist in diesem Lauf geräumt** — nachgemessen: `unterminierte_tickets` = 0,
`plandrift` = 0.

### Gebaut ist nicht der Lauf, sondern die Fähigkeit der Registry, über sich Auskunft zu geben

**`SWR-212`.** Gemessen: **9 von 9** ollama-Einträgen in den drei Run-Registries tragen
`"provider": "", "modell": ""` — der Fehlerausgang von `gateway.core.execute` baute sein
`Ergebnis` ohne beide Felder. Die Wahrheit stand nur in `meldung`, dem einzigen Feld, das
keine Auswertung dieses Hauses liest. **Genau daher kommen die drei falschen Diagnosen.**

6 Zusicherungen in Paarform, Mutationsprobe gegen den alten Fehlerausgang und gegen eine
unbefüllte Versuchsliste: **je 3 rot**.

### ⚠ Warum `in_review` und nicht `done`

Die DoD verlangt **einen echten Lauf**. Den kann diese Session nicht führen — er entsteht
auf dem Rechner des Auftraggebers, im 15-Minuten-Takt, von allein. Das Ticket jetzt zu
schließen wäre die bequeme Antwort auf drei Sprints Verschiebung.

> **Ein Nachweis, der nicht geführt wurde, wird nicht dadurch geführt, dass die Ursache
> verstanden ist.** (Dieselbe Regel wie bei `pm/T-0010`/`T-0013`.)

**Stichprobe des Auftraggebers:** Nach dem nächsten Schnelltakt-Lauf steht in
`ollama-schnelltakt.log` **`PREFLIGHT: STARTKLAR`** statt „Preflight hat Befunde", und
danach entweder eine `Gateway:`-Zeile (dann ist der Nachweis geführt) oder
`Kein bearbeitbares Ticket (Besetzung)` — dann beißt **Ursache 3**, und die steht als
`platform/T-0066` bereits geschrieben.

### ⚠ Die dritte Ursache, neu benannt und abgetrennt

Auch mit geräumtem Preflight hat der Takt möglicherweise **nichts zu tun**: die beiden
ollama-besetzten Instanzen (`PROB@platform`, `MAIL-RED@team-mail`) haben zusammen **null**
offene Tickets, während `roles/registry.yaml` für `CM`, `PL` und `COACH` ollama-Ketten
führt. Zwei Register, eine Frage, zwei Antworten. → **`platform/T-0066`**, Sprint 36.
**Nicht hier mitentschieden:** Besetzungen sind Klasse B des PM.
