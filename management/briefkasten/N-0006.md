---
von: E. John
zeit: 2026-08-16T08:36:38+00:00
status: beantwortet
---

git commit bug: 
pm/T-0024 (SUP.9): git_prozess_aktiv liest tasklist jetzt mit errors=… Annotations
1 error
tests
Process completed with exit code 1.ci.yml

---

## Antwort des Teams (Routine-Session, 2026-08-16)

**Nachgesehen — und lokal nicht nachstellbar.** Alle drei CI-Schritte aus
`platform/.github/workflows/ci.yml` mit demselben Repo-Layout wie der Workflow (p0…p9, pm,
process, produkt-datakonv neben `platform`) nachgestellt:

- `python -m unittest discover tests -v` → **268 Tests, alle grün**
- `trace_matrix.py --check --alle-projekte` → 87 SWRs, 0 Lücken
- `catalog.py --check` → Katalog konsistent

Damit lässt sich der Fehler am aktuellen Stand nicht reproduzieren. Zwei ehrliche
Einschränkungen dazu: Uns liegt nur die GitHub-Kurzmeldung vor („1 error", „exit code 1"),
nicht die eigentliche Fehlerzeile aus dem Protokoll — und wir prüfen auf Python 3.11 (wie
die CI), lokal aber ohne 3.11 im Sandbox, also auf 3.10.

**Naheliegendste Erklärung:** Der Auto-Push-Wächter war bis kurz vor diesem Lauf gestört
(`pm/T-0024`) und hat beim ersten Durchlauf danach mehrere Repos nacheinander nachgeholt.
Löste das den `platform`-CI-Lauf aus, bevor `pm`/`process` durch waren, prüfte GitHub
kurzzeitig eine **ältere** Fassung der Geschwister-Repos — derselbe Mechanismus wie bei
`pm/T-0013` im Sommer. Das legt sich von selbst, sobald alle Repos durchgepusht sind, was
inzwischen der Fall sein sollte.

**Verbucht als `platform/T-0002`, bleibt `in_review`** — kein Fix ins Blaue ohne
reproduzierten Fehler (Lesson aus `pm/T-0023`). Schließt, sobald ein aktueller Lauf grün
ist. **Falls der Fehler erneut auftritt: die tatsächliche Fehlerzeile aus dem
Actions-Protokoll (nicht nur die Kurzmeldung) weiterleiten** — damit lässt sich gezielt
suchen statt raten.
